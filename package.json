{
  "name": "kasmiri-hospital",
  "version": "1.0.0",
  "scripts": {
    "build": "npm run build --prefix frontend && mkdir -p frontend/dist/media && (cp -rf backend/media/* frontend/dist/media/ || true)",
    "install": "cd frontend && npm install"
  }
}
