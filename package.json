{
  "name": "kasmiri-hospital",
  "version": "1.0.0",
  "scripts": {
    "build": "cd frontend && npm install && npm run build && mkdir -p dist/media && cp -r ../backend/media/* dist/media/",
    "install": "cd frontend && npm install"
  }
}
